{
  "name": "runhub",
  "version": "0.3.1",
  "description": "One coding agent in a worktree, mechanical verify, optional Claude review, phone report.",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/0jrm/runhub.git"
  },
  "type": "module",
  "bin": {
    "runhub": "./dist/cli.js"
  },
  "files": [
    "dist",
    "README.md",
    "GROKBOT.md",
    "LICENSE"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsc && chmod +x dist/cli.js",
    "prepack": "npm run build",
    "test": "rm -rf dist-test && tsc && tsc -p tsconfig.test.json && node --test dist-test/test/*.test.js",
    "contract": "rm -rf dist-test && tsc && tsc -p tsconfig.test.json && node dist-test/test/contract.js",
    "pack:tarball": "npm pack"
  },
  "devDependencies": {
    "@types/node": "^22.13.0",
    "typescript": "^5.8.0"
  }
}
